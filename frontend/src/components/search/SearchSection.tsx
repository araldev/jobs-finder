/**
 * Search section — T-007 replaces this with the full search UI
 * (SearchBar, ResultsGrid, JobCard, EmptyState, ErrorState,
 * Skeletons, useDebouncedJobsSearch). The placeholder keeps the
 * build green and shows a hint of what is coming.
 */
export function SearchSection(): React.ReactElement {
  return (
    <div className="rounded-2xl border border-dashed border-border/60 bg-card/40 p-6 text-sm text-muted-foreground">
      Search section — full UI lands in T-007.
    </div>
  );
}
