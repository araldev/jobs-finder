# Landing assets

Drop the actual CV preview GIF/PNG here as `cv-preview.png` (or `.gif`).
Until then, the landing shows the "próximamente" placeholder.
Recommended: ~1280x720, max 1 MB, first-frame of the CV upload → adapted CV demo.