# frontend

Placeholder for the jobs-finder web client. **Not implemented yet.**

This directory will hold the user-facing application that talks to the
backend in `../backend/`. The backend exposes a FastAPI HTTP API
documented in `../backend/README.md` — once a frontend stack is chosen
(Next.js, Vite + React, SvelteKit, etc.), this `README.md` will be
replaced with the project's own README covering setup, scripts, and
architecture.

Until then, keep this directory empty except for tooling files
(`.gitignore`, `package.json`, etc.) once a stack is picked.
