"""Routes mounted by the FastAPI app (`/health`, `/jobs/linkedin`).

The actual router instances are defined in their own submodules; this
package only exists so FastAPI can include them via `app.include_router`.
"""
